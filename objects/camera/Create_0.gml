following = obj_player;  // Object to follow
hBorder = 60; // Border for horizontal (follow object if player is within this number on both sides (done in step event)
vBorder = 30; // Border for vertical (follow object if player is within this number on both top and bottom (done in step event)

